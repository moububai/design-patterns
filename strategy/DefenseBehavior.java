package strategy;
/*
 * Interface for the Player class that derives uses the following classes:
 * BlockBehavior
 * StripBehavior
 * SackBehavior
 */
interface DefenseBehavior
{
    String play();
    
}
