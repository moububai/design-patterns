package strategy;
/*
 * Interface for the Player class that derives uses the following classes:
 * OBlockBehavior
 * RunBehavior
 * PassBehavior
 * RecieveBehavior
 */

interface OffenseBehavior
{
    String play();
    
}
